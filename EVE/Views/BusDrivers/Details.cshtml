@model BusDriver

@{
    ViewData["Title"] = "Details of the drivers";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Details for @Model.Emri</h1>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">
                <div class="form-group text-center">
                    <img class="border-info rounded-circle" style="max-width: 150px" id="ProfilePicturePreview" />
                </div>
                <div class="form-group">
                    <label asp-for="BusDriverPic" class="control-label"></label>
                    <input asp-for="BusDriverPic" class="form-control" readonly />
                    <span asp-validation-for="BusDriverPic" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Emri" class="control-label"></label>
                    <input asp-for="Emri" class="form-control" readonly />
                    <span asp-validation-for="Emri" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Bio" class="control-label"></label>
                    <input asp-for="Bio" class="form-control" readonly />
                    <span asp-validation-for="Bio" class="text-danger"></span>
                </div>

                <div class="form-group">
                    @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                    {
                        <a class="btn btn-outline-success float-right" asp-action="Edit" asp-route-id="@Model.ID">Edit</a>
                    }


                    <a class="btn btn-outline-secondary" asp-action="Index">Show All</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            var output = document.getElementById('ProfilePicturePreview');
            output.src = $("#BusDriverPic").val();
        })
    </script>
}